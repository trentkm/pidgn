//
//  Theme.swift
//  Pidgn
//
//  Earth-toned palette. Warm, minimal, native.

import SwiftUI

enum PidgnTheme {
    // Primary accent — warm terracotta clay
    static let accent = Color(red: 0.78, green: 0.49, blue: 0.36)

    // Secondary — sage green (sparingly, for success/connected states)
    static let sage = Color(red: 0.55, green: 0.65, blue: 0.52)

    // Warm neutral for subtle backgrounds
    static let sand = Color(red: 0.96, green: 0.93, blue: 0.89)
}
